// Test configuration for code patching by symbol name
{
    elf: "tests/bin/test.elf",
    class: [
        "single",
        "glitch",
    ],
    analysis: true,
    code_patches: [
        // Patch check_secret function to return immediately with success (return 1)
        {
            symbol: "check_secret",
            data: "0x47702001",  // bx lr; movs r0, #1
        },
    ],
}
